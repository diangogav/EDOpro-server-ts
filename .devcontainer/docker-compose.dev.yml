version: '3.8'

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    volumes:
      - ../:/workspace:cached
    command: /bin/sh -c "while sleep 1000; do :; done"
    ports:
      - "4000:4000"
      - "7911:7911"
      - "7922:7922"
      - "7711:7711"
